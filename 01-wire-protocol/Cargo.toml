[package]
name = "wire_protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
hmac = "0.12"
subtle = "2"

[dev-dependencies]
proptest = "1"

